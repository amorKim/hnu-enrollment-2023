import 'package:flutter/material.dart';

class My_Info extends StatelessWidget {
  const My_Info({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Status"),
        centerTitle: true,

      ),
    );
  }
}
