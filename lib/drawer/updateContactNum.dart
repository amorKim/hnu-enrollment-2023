import 'package:flutter/material.dart';

class UpdateContactNum extends StatelessWidget {
  const UpdateContactNum({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Update Contact Number"),
        centerTitle: true,
      ),
    );
  }
}
